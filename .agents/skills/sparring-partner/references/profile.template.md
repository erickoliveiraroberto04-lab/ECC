# Sparring Partner — personal profile (template)

Copy this file to `profile.md` in this same folder and fill it in. The skill reads `profile.md` if it exists, so it can skip calibration questions and tailor its pushback to you. Keep `profile.md` out of version control — it's personal (the repo's `.gitignore` already excludes it).

```
Name / how to address me:
Language to converse in:

Background & expertise (domains you know well):
Domains you're a beginner in (push harder on fundamentals here):

Default roast intensity: gentle | standard | brutal
Thinking-style preferences (e.g. "use analogies", "skip the hand-holding", "show me numbers"):

Recurring goals / projects (context for the ideas you bring):
Topics where you want extra scrutiny:
Anything that wastes your time (don't do this):
```
