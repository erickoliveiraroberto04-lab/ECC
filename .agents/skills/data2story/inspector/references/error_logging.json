{
  "_note": "Optional. Running verify.py with --log-errors auto-updates known recurring-case metadata in skills/errors/ for patterns it can detect from HTML. Use manual logging only when you discover a NEW pattern the script does not know yet, and only for Critical/High-severity issues that represent patterns (not one-off mistakes).",
  "process": [
    "1. Check if ../errors/ directory exists.",
    "2. If yes, search for similar errors: grep -r \"keyword\" ../errors/cases/.",
    "3. If a similar error exists: read the existing case file, increment frequency in frontmatter, update last_seen to today, add context if needed.",
    "4. If new: create ../errors/cases/[type]_[timestamp].md using the template below, then update ../errors/index.md to include the new error type in the appropriate category."
  ],
  "case_template": [
    "---",
    "type: descriptive_error_type",
    "tags: relevant, keywords, for, grep",
    "frequency: 1",
    "last_seen: YYYY-MM-DD",
    "severity: critical|high|medium|low",
    "---",
    "",
    "# Error: Brief title",
    "",
    "## Context",
    "What was the programmer trying to do?",
    "",
    "## Symptom",
    "What went wrong? How did you detect it?",
    "",
    "## Example (Wrong)",
    "```html",
    "<!-- Show the incorrect code -->",
    "```",
    "",
    "## Fix",
    "```html",
    "<!-- Show the correct code -->",
    "```",
    "",
    "## Prevention",
    "Step-by-step guidance to avoid this error in the future.",
    "",
    "## Root Cause",
    "Why does this error happen? What's the underlying issue?"
  ],
  "only_log_recurring": "Only log errors likely to recur — skip one-off typos, data-specific issues, or problems caused by malformed input files. Focus on implementation patterns the programmer should learn to avoid.",
  "common_error_types": [
    "Missing traceability attributes (data-des, data-ana, data-edt)",
    "Chart data not matching analyst.json",
    "Layout issues (overlap, no wrapper, incorrect spacing)",
    "Broken asset paths",
    "Interactive elements without affordances",
    "Missing or incorrect data resolution from designer → analyst"
  ]
}
